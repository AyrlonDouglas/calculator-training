import { createTheme } from "@mui/material"

export const theme = createTheme({
	palette: {
		primary: { main: "#1a759f" },
		secondary: { main: "#99d98c" },
		error: { main: "#e07a5f" },
		warning: { main: "#ffd60a" },
	},
	components: {},
	typography: {
		htmlFontSize: 10,
	},
})
